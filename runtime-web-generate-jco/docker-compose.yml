version: "3.7"

services:
  wit-runtime-wasi-web-generate-jco:
    image: nginx:latest
    container_name: wit-runtime-wasi-web-generate-jco
    ports:
      - "8100:80"
    volumes:
      - .:/usr/share/nginx/html
    networks: []
    restart: unless-stopped

networks: {}
