version: "3.7"

services:
  wit-runtime-wasi-web-jco:
    image: nginx:latest
    container_name: wit-runtime-wasi-web-jco
    ports:
      - "8099:80"
    volumes:
      - .:/usr/share/nginx/html
    networks: []
    restart: unless-stopped

networks: {}
